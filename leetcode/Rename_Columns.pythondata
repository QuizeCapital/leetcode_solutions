import pandas as pd

def renameColumns(students: pd.DataFrame) -> pd.DataFrame:
    cols_new = {'id': 'student_id', 'first': 'first_name', 'last': 'last_name', 'age': 'age_in_years'}
    students.rename(columns = cols_new, inplace = True)
    return students
    