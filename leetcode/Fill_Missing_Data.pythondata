import pandas as pd

def fillMissingValues(products: pd.DataFrame) -> pd.DataFrame:
    # products['quantity'] = np.where(products['quantity'].isna(), 0 , products['quantity'])
    products['quantity'] = products['quantity'].fillna(0)

    return products
    