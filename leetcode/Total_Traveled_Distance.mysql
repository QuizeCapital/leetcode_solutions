# Write your MySQL query statement below
select 
u.user_id
,u.name
, case when sum(distance) is null then 0 else sum(distance) end as 'traveled distance'
from Users u 
left join Rides r on u.user_id = r.user_id
group by u.user_id 
order by u.user_id asc